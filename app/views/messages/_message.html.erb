<div class="send-message-details">
  <div class="s-image">
    <%= avatar(message.sender) %>
  </div>

  <div class="s-about">
    <h4 class="s-heading">
      <% if message.sender == current_user %>
        Me
      <% else %>
        <%= message.sender.name %>
      <% end %>
    </h4>
    <span class="s-time-date pull-right"><%= timeago_tag(message.created_at) %></span>
    <%= simple_format(sanitize(message.body)) %>
  </div>
  <div class="clr"></div>
</div>
